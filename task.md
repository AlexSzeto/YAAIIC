# Sound Editor — Clip Playback Fixes

## Goal

Address the remaining issues with the brew editor.

## Tasks

- [x] Update the custom-ui audio player to optionally support region parameters, which will allow for the playback of a specific region of the audio file. Update the clips in the dynamic list to pass the playback region parameters to the audio player.
- [x] Remove the browse button and its handler from the dynamic list clip UI.
- [x] Remove the upload button and its handler from the sound source form.
